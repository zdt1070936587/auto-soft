# 本地覆盖示例。复制为 application-local.yml（已 gitignore）后按需修改。
# 若 5432 被占用，把端口改成与 docker-compose 映射一致。
#
# server:
#   port: 8080
# spring:
#   datasource:
#     url: jdbc:postgresql://127.0.0.1:5432/autosoft
#     username: autosoft
#     password: autosoft
